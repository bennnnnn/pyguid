---
import CodeExample from "./CodeExample.astro";

interface Props {
  code: string;
  filename?: string;
  runnable?: boolean;
}

const { code, filename = "example.py", runnable = true } = Astro.props;
---

<CodeExample code={code} filename={filename} language="python" runnable={runnable} />
