---
import "../styles/global.css";
import TutorialTopNav from "../components/TutorialTopNav.astro";
import TutorialFooter from "../components/TutorialFooter.astro";
import SignInModal from "../components/SignInModal.astro";
import { SITE, getSiteUrl } from "../lib/site";
import SeoHead from "../components/SeoHead.astro";

interface Props {
  title?: string;
  description?: string;
}

const { title = `${SITE.name} — Learn Python for free`, description = SITE.description } =
  Astro.props;

const canonical = new URL(Astro.url.pathname, `${getSiteUrl(Astro.site)}/`).href;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <SeoHead title={title} description={description} canonical={canonical} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap"
      rel="stylesheet"
    />
    <title>{title}</title>
  </head>
  <body class="flex min-h-screen flex-col bg-surface">
    <TutorialTopNav />
    <main class="flex-1">
      <slot />
    </main>
    <TutorialFooter />
    <SignInModal />
  </body>
</html>
