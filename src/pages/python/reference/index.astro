---
import TutorialLayout from "../../../layouts/TutorialLayout.astro";
import { getReferenceMenuItems } from "../../../lib/reference-sheets";
import { SITE } from "../../../lib/site";
import "../../../styles/reference.css";

const entryCount = getReferenceMenuItems().length;
---

<TutorialLayout
  title={`Python Reference — ${SITE.name}`}
  description="Python syntax reference — operators, built-in functions, string and list methods, and common patterns. Look up what you need while coding."
  sidebar="reference"
>
  <article class="lesson-prose-wide lesson-prose">
    <h1 class="!mt-0">Python reference</h1>
    <p class="text-text-secondary">
      Quick lookup for <strong>syntax</strong>, <strong>built-in functions</strong>, and
      common <strong>methods</strong>. Pick a topic in the sidebar — each item opens its own
      page with a short explanation and example.
    </p>
    <p class="text-sm text-text-muted">
      {entryCount} entries · Runnable examples where Python runs in the browser
    </p>
  </article>
</TutorialLayout>
