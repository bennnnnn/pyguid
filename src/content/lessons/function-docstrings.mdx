---
title: "Docstrings"
description: "Document functions with triple-quoted docstrings Learn Docstrings in this Functions lesson with runnable Python examples on PyGuide. Beginner-friendly..."
order: 144
chapter: 21
chapterTitle: "Functions"
part: 6
partTitle: "Functions"
level: beginner
quickAnswer: "Document functions with triple-quoted docstrings. This PyGuide lesson shows Docstrings with examples you can run in the browser."
---

## Why this matters

Docstrings shows up throughout Functions and later chapters. Understanding it here makes the next lessons easier and helps you read real Python code with confidence. PyGuide keeps each page focused on one idea so you can practice without getting lost.

A sticky note on a recipe card explains what the dish is for. A **docstring** is that note on a function — the first string right under **`def`**.

## One new idea: triple quotes under def

<PythonExample
  filename="doc.py"
  code={`def area(width, height):
    """Return rectangle area (width * height)."""
    return width * height

print(area.**doc**)
print(area(4, 5))`}
/>

**Line by line**

- `"""..."""` — docstring (usually one short sentence)
- `area.__doc__` — read the text (tools like `help(area)` use this too)

## Real use case: explain units

<PythonExample
  filename="celsius.py"
  code={`def to_fahrenheit(celsius):
    """Convert Celsius to Fahrenheit."""
    return celsius * 9 / 5 + 32

print(to_fahrenheit(0))`}
/>

<Callout variant="tip">
  Say **what** the function returns, not every line of how — unless the how is surprising.
</Callout>

## Recap

- First string in function body = **docstring**
- Helps **you** and teammates months later
