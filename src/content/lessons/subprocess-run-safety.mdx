---
title: "subprocess.run and shell safety"
description: "Run external commands without shell injection risks Learn subprocess.run and shell safety in this Modules lesson with runnable Python examples on PyGuide...."
order: 141
chapter: 28
chapterTitle: "Modules"
section: "subprocess preview"
part: 7
partTitle: "Files, modules, and the standard library"
level: beginner
quickAnswer: "Run external commands without shell injection risks. This PyGuide lesson shows subprocess.run and shell safety with examples you can run in the browser."
---

## Why this matters

subprocess.run and shell safety shows up throughout Modules and later chapters. Understanding it here makes the next lessons easier and helps you read real Python code with confidence. PyGuide keeps each page focused on one idea so you can practice without getting lost.

Sometimes Python calls another program — `git status`, `python -m pytest`, a formatter. **`subprocess.run`** starts that process and waits for the result.

## One new idea: list form (safe)

```python
import subprocess

result = subprocess.run(
    ["python", "--version"],
    capture_output=True,
    text=True,
)
print(result.stdout)
print("Exit code:", result.returncode)
```

Pass the command as a **list of strings** — no shell interprets spaces or `;`.

## Simulated result (browser)

<PythonExample
  filename="subprocess_demo.py"
  code={`# What subprocess returns conceptually
class Result:
    def __init__(self):
        self.returncode = 0
        self.stdout = "Python 3.12.0\\n"

result = Result()
print(result.stdout)
print("Exit code:", result.returncode)`}
/>

## Avoid shell=True

| Style                           | Risk                             |
| ------------------------------- | -------------------------------- |
| `run(["git", "status"])`        | safer                            |
| `run("git status", shell=True)` | injection if user input mixed in |

If you must use the shell, never paste untrusted text into the command string.

<Callout variant="danger" title="Oops — Python is yelling">
  User input in `shell=True` strings can run **arbitrary** commands. Use the **list
  form**.
</Callout>

## Recap

- **`subprocess.run([...])`** with a list
- **`capture_output=True, text=True`** for stdout
- Avoid **`shell=True`** unless you understand the risk
