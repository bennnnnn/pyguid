---
title: "sort()"
description: "Python list sort() orders items in place; sorted() returns a new list. Beginner tutorial covers ascending, descending, key functions, and stable sorting."
order: 69
chapter: 10
chapterTitle: "Lists"
level: beginner
quickAnswer: "Call list.sort() to sort a list in place, or sorted(list) to get a new sorted list without changing the original."
faqs:
  - question: "What is the difference between sort() and sorted()?"
    answer: "sort() modifies the list in place and returns None. sorted() returns a new sorted list and leaves the original unchanged."
  - question: "How do I sort a list in reverse order?"
    answer: "Use list.sort(reverse=True) or sorted(my_list, reverse=True) to sort from largest to smallest."
  - question: "Can I sort a list of strings by length?"
    answer: "Yes. Pass key=len: names.sort(key=len) sorts by string length while keeping the original strings intact."
  - question: "Does sort() work on lists with mixed types?"
    answer: "Not reliably. Python 3 compares compatible types, but mixing numbers and strings usually raises TypeError. Normalize types first."
---

## Why this matters

Sorting turns raw data into readable reports, leaderboards, and search results. sort() vs sorted() trips up beginners who accidentally mutate shared lists. The key= argument unlocks sorting by length, date, or custom rules without rewriting data.

High scores on a leaderboard should go **low → high** (or the other way). **`sort()`** rearranges the **same list** so items are in order — like sorting cards in your hand without getting a second deck.

## One new idea: sort in place

<PythonExample
  filename="sort.py"
  code={`scores = [88, 72, 95, 64, 85]
scores.sort()
print(scores)`}
/>

**Before → after**

| Before                 | After `sort()`         |
| ---------------------- | ---------------------- |
| `[88, 72, 95, 64, 85]` | `[64, 72, 85, 88, 95]` |

Default: **smallest → largest**.

## Biggest first

<PythonExample
  filename="sort_reverse.py"
  code={`nums = [3, 1, 4, 1, 5]
nums.sort(reverse=True)
print(nums)`}
/>

## Sort words (case matters)

<PythonExample
  filename="sort_strings.py"
  code={`names = ["Charlie", "ada", "Ben"]
names.sort()
print(names)`}
/>

Capital letters sort **before** lowercase in ASCII order (`"Ben"` before `"ada"`). For friendlier order:

<PythonExample
  filename="sort_key.py"
  code={`names = ["Charlie", "ada", "Ben"]
names.sort(key=str.lower)
print(names)`}
/>

## sorted() — keep the original order too

<PythonExample
  filename="sorted.py"
  code={`original = [3, 1, 2]
ordered = sorted(original)
print("original:", original)
print("ordered:", ordered)`}
/>

| Tool       | Changes original? | Returns      |
| ---------- | ----------------- | ------------ |
| `.sort()`  | Yes               | `None`       |
| `sorted()` | No                | **new** list |

## Common mistake: expecting a return from sort()

<Callout variant="warning" title="Oops — Python is yelling">
  **`result = scores.sort()`** sets **`result`** to **`None`**. The sorted data is in
  **`scores`**, not in `result`.
</Callout>

<Callout variant="danger">
  **`[1, "two"].sort()`** → **TypeError**. Python cannot compare number and text to order
  them.
</Callout>

## Recap

- **`list.sort()`** — reorder **this** list; returns **`None`**
- **`sorted(list)`** — new sorted list; original unchanged
- **`reverse=True`** — largest first
