---
title: "readlines() and encoding"
description: "Read files line by line and specify text encoding Learn readlines() and encoding in this Files lesson with runnable Python examples on PyGuide...."
order: 149
chapter: 25
chapterTitle: "Files"
section: "Reading files"
part: 7
partTitle: "Files, modules, and the standard library"
level: beginner
quickAnswer: "Read files line by line and specify text encoding. This PyGuide lesson shows readlines() and encoding with examples you can run in the browser."
---

## Why this matters

readlines() and encoding shows up throughout Files and later chapters. Understanding it here makes the next lessons easier and helps you read real Python code with confidence. PyGuide keeps each page focused on one idea so you can practice without getting lost.

Big files are easier **one line at a time**. **`readlines()`** returns a list of lines; **`encoding="utf-8"`** tells Python how to decode bytes into text.

## One new idea: lines as a list

<PythonExample
  filename="lines_sim.py"
  code={`# Simulated file content
content = "alpha\\nbeta\\ngamma\\n"
lines = content.splitlines()

for line in lines:
print(repr(line))`}
/>

On your PC:

```python
with open("notes.txt", encoding="utf-8") as f:
    for line in f:
        print(line.strip())
```

Looping the file object is **memory-friendly** for huge files.

## encoding parameter

| Issue                | Fix                                                    |
| -------------------- | ------------------------------------------------------ |
| `UnicodeDecodeError` | open with **`encoding="utf-8"`** (or correct encoding) |
| Weird symbols        | wrong encoding guessed                                 |

<Callout variant="note">
  PyGuide simulates text in memory. Practice **`encoding=`** on your machine with real
  files.
</Callout>

## Recap

- **`readlines()`** / **loop file** — line by line
- **`encoding="utf-8"`** — standard for text
- **`strip()`** — remove newline characters when printing
