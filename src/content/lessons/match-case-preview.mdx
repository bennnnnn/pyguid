---
title: "match / case (Python 3.10+)"
description: "Branch on values with structural pattern matching Learn match / case (Python 3.10+) in this Python Standard Library lesson with runnable Python examples on..."
order: 168
chapter: 29
chapterTitle: "Standard Library Essentials"
section: "match case preview"
part: 7
partTitle: "Files, modules, and the standard library"
level: beginner
quickAnswer: "Branch on values with structural pattern matching. This PyGuide lesson shows match / case (Python 3.10+) with examples you can run in the browser."
---

## Why this matters

match / case (Python 3.10+) shows up throughout Python Standard Library and later chapters. Understanding it here makes the next lessons easier and helps you read real Python code with confidence. PyGuide keeps each page focused on one idea so you can practice without getting lost.

Long **`if` / `elif`** chains on one variable get noisy. **`match`** / **`case`** (Python 3.10+) express “when this shape, do that” — especially nice for **strings** and **simple patterns**.

## One new idea: match command

<PythonExample
  filename="match_demo.py"
  code={`def handle(command):
    match command:
        case "quit":
            return "Goodbye"
        case "help":
            return "Commands: help, quit"
        case _:
            return "Unknown command"

print(handle("help"))
print(handle("fly"))`}
/>

**Line by line**

- `match command:` — what you are inspecting
- `case "quit":` — exact string match
- `case _:` — wildcard (like `else`)

## Multiple values in one case

<PythonExample
  filename="match_multi.py"
  code={`def http_label(code):
    match code:
        case 200 | 201:
            return "OK"
        case 404:
            return "Not found"
        case _:
            return "Other"

print(http_label(200))
print(http_label(500))`}
/>

`|` inside a case means “or”.

<Callout variant="note">
  Need Python **3.10+**. Older versions stick with **`if` / `elif`** — still fine.
</Callout>

## Recap

- **`match`** + **`case`** for clear branching
- **`case _:`** is the default
- Optional upgrade from **elif** chains
