---
title: "split()"
description: "Python string split() breaks text into a list by separator. Beginner tutorial covers default whitespace splits, maxsplit, and parsing CSV-like lines safely."
order: 27
chapter: 7
chapterTitle: "Strings"
level: beginner
quickAnswer: "Call text.split() to split on whitespace into a list of words, or text.split(',') to split on a specific delimiter string."
faqs:
  - question: "What does str.split() do in Python?"
    answer: "split() divides a string into a list of substrings using a separator. With no argument it splits on any whitespace."
  - question: "How do I split a CSV line by commas?"
    answer: "Use line.split(',') for simple cases. For quoted fields with commas inside, use the csv module instead."
  - question: "What does the maxsplit argument do?"
    answer: "split(sep, maxsplit=n) performs at most n splits, leaving the remainder in the final list element."
  - question: "Does split() modify the original string?"
    answer: "No. Strings are immutable. split() returns a new list and leaves the original string unchanged."
---

## Why this matters

split() is the first tool for parsing user input, file lines, and URLs into workable pieces. Wrong delimiter assumptions create silent data corruption. Combining split with strip and join covers most beginner text-processing tasks.

A CSV row looks like one long string: `apple,banana,cherry`. **`split()`** is the knife that cuts it into separate pieces — a **list** of strings.

## One new idea: split on whitespace

With **no argument**, `split()` breaks on spaces, tabs, and newlines:

<PythonExample
  filename="split.py"
  code={`sentence = "Python is fun"
words = sentence.split()
print(words)`}
/>

**Before → after**

| `sentence`        | `words`                   |
| ----------------- | ------------------------- |
| `"Python is fun"` | `["Python", "is", "fun"]` |

## Split on a specific character

<PythonExample
  filename="split_comma.py"
  code={`csv = "apple,banana,cherry"
fruits = csv.split(",")
print(fruits)`}
/>

## Limit how many cuts

<PythonExample
  filename="maxsplit.py"
  code={`data = "one-two-three-four"
parts = data.split("-", 2)
print(parts)`}
/>

Only **2** splits → `["one", "two", "three-four"]` — the rest stays in the last piece.

## Real use case: read a data line

<PythonExample
  filename="user_row.py"
  code={`row = "ada,36,london"
parts = row.split(",")
name = parts[0]
age = parts[1]
city = parts[2]
print(name, "is", age, "from", city)`}
/>

<Callout variant="note">
  **`split()`** returns a **list**. Use **`join()`** (next lesson) to glue pieces back.
</Callout>

<TryIt title="Try it yourself" answer='["a", "b", "c"]'>
  With `s = "a,b,c"`, print `s.split(",")`.
</TryIt>

## Recap

- **`split()`** — default: whitespace
- **`split(",")`** — cut on commas
- Second argument limits number of splits

Next: **`join()`** — combine a list into one string.
