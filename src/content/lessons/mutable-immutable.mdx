---
title: "Mutable vs immutable"
description: "Which Python types can change in place and which cannot Learn Mutable vs immutable in this Data Types lesson with runnable Python examples on PyGuide...."
order: 18
chapter: 9
chapterTitle: "Data Types"
section: "Mutable vs immutable"
part: 3
partTitle: "Types, numbers, and strings"
level: beginner
quickAnswer: "Which Python types can change in place and which cannot. This PyGuide lesson shows Mutable vs immutable with examples you can run in the browser."
---

## Why this matters

Mutable vs immutable shows up throughout Data Types and later chapters. Understanding it here makes the next lessons easier and helps you read real Python code with confidence. PyGuide keeps each page focused on one idea so you can practice without getting lost.

A whiteboard you can erase is **mutable**. A tattoo is **immutable** — you make a new design instead of editing ink in the skin. Python types split the same way.

## One new idea: two families

| Mutable (can change in place) | Immutable (cannot)     |
| ----------------------------- | ---------------------- |
| `list`                        | `int`, `float`, `bool` |
| `dict`                        | `str`                  |
| `set`                         | `tuple`                |

<PythonExample
  filename="demo.py"
  code={`nums = [1, 2, 3]
nums.append(4)
print("list changed:", nums)

text = "hi"

# text[0] = "H" # TypeError if uncommented

text = "Hello"
print("new string:", text)`}
/>

Lists **grow**; strings need a **new** string assigned.

## Why it matters: shared references

<PythonExample
  filename="alias.py"
  code={`a = [1, 2]
b = a
b.append(3)
print("a:", a)`}
/>

Mutable + two names = **one list**, both see changes.

<Callout variant="tip">
  **Immutable** values are safer as dict **keys** (you will use strings, numbers, tuples).
</Callout>

## Recap

- **Mutable** — edit in place (`list`, `dict`, `set`)
- **Immutable** — create new value (`str`, `tuple`, numbers)
- Explains **aliasing** surprises with lists
