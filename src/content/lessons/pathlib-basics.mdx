---
title: "pathlib basics"
description: "Python pathlib basics: Path objects for files and folders with / syntax. Beginner tutorial on joining paths, reading text, and listing directories."
order: 164
chapter: 26
chapterTitle: "Pathlib"
part: 7
partTitle: "Files, modules, and the standard library"
level: beginner
quickAnswer: "Use Path from pathlib and / to build paths portably, e.g. Path('data') / 'file.txt', instead of manual string concatenation with os.path."
---

## Why this matters

Scripts touch files constantly—logs, CSV exports, config folders. pathlib avoids Windows vs Unix slash bugs and reads cleaner than os.path.join chains. It is the modern default for new Python file code.

String paths get messy with slashes. **`pathlib`** (standard library) treats paths as objects — cleaner joins and checks.

## One new idea: Path objects

On your computer:

```python
from pathlib import Path

folder = Path("data")
file = folder / "notes.txt"
print(file)
print(file.name)
print(file.suffix)
```

| Property / op            | Example result |
| ------------------------ | -------------- |
| `Path("data") / "a.txt"` | `data/a.txt`   |
| `.name`                  | `notes.txt`    |
| `.suffix`                | `.txt`         |

## Simulate the idea

<PythonExample
  filename="path_sim.py"
  code={`# pathlib needs a real filesystem — concept demo
parts = ["data", "notes.txt"]
joined = "/".join(parts)
print("Path string:", joined)
print("Filename:", "notes.txt")
print("Extension:", ".txt")`}
/>

## Why pathlib?

- **`/` operator** joins paths (OS-aware on real `Path`)
- **`.exists()`**, **`.read_text()`** — readable methods (local Python 3)

<Callout variant="tip">
  New projects often prefer **`pathlib`** over manual string paths with **`os.path`**.
</Callout>

## Recap

- **`from pathlib import Path`**
- **`folder / "file.txt"`** — join paths
- Use on your PC for real file work
