---
title: "strip()"
description: "Remove whitespace with strip(), lstrip(), and rstrip() Learn strip in this Strings lesson with runnable Python examples on PyGuide. Beginner-friendly..."
order: 61
chapter: 11
chapterTitle: "Strings"
part: 3
partTitle: "Types, numbers, and strings"
level: beginner
quickAnswer: "Remove whitespace with strip(), lstrip(), and rstrip(). This PyGuide lesson shows strip with examples you can run in the browser."
---

## Why this matters

strip shows up throughout Strings and later chapters. Understanding it here makes the next lessons easier and helps you read real Python code with confidence. PyGuide keeps each page focused on one idea so you can practice without getting lost.

Someone hands you a sticky note with spaces smeared around the words. **`strip()`** trims that junk from the **start and end** — not from the middle of the sentence.

## One new idea: strip() both ends

<PythonExample
  filename="strip.py"
  code={`text = "   Hello, Python!   "
clean = text.strip()
print(repr(clean))`}
/>

**Before → after**

| `text` (with spaces) | `clean` after `strip()` |
| -------------------- | ----------------------- |
| `"   Hello...   "`   | `"Hello, Python!"`      |

`repr()` shows quotes so you can **see** spaces are gone.

## One side only: lstrip() and rstrip()

<PythonExample
  filename="sides.py"
  code={`line = ">>> prompt "
print(line.lstrip("> "))
print(line.rstrip())`}
/>

| Method     | Removes from   |
| ---------- | -------------- |
| `lstrip()` | **left** start |
| `rstrip()` | **right** end  |

## Real use case: clean form input

<PythonExample
  filename="input_clean.py"
  code={`raw = "  yes  "
cleaned = raw.strip().lower()
print(cleaned)`}
/>

Pattern: **`strip()`** then **`lower()`** before comparing answers.

<Callout variant="tip">
  Always **`strip()`** text from forms, files, or copy-paste before **`==`** comparisons.
</Callout>

## Recap

- **`strip()`** — both ends
- **`lstrip()` / `rstrip()`** — one side
- Middle spaces stay unless you use other tools
