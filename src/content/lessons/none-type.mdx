---
title: "None"
description: "Python None represents no value or a missing result. Beginner tutorial covers NoneType, is None checks, default returns, and why None is not the string 'None'."
order: 51
chapter: 8
chapterTitle: "Data Types"
part: 3
partTitle: "Types, numbers, and strings"
level: beginner
quickAnswer: "None is Python's null value—a singleton object meaning nothing here; test with is None, not ==, when checking for absence."
---

## Why this matters

Functions without return implicitly return None; APIs use None for optional fields and not found results. Confusing None with 0, False, or empty strings breaks validation logic. Correct None checks prevent crashes when chaining method calls on missing data.

An empty slot on a form — not zero, not false, just **blank**. **`None`** is Python’s way to say “no value here” or “not found.”

## One new idea: None

<PythonExample
  filename="none.py"
  code={`result = None
print(result)
print(type(result))`}
/>

**Line by line**

- `None` — a single shared object meaning “nothing”
- `type(result)` — `<class 'NoneType'>`
- Different from `0`, `False`, or `""`

## Functions without return give None

<PythonExample
  filename="no_return.py"
  code={`def log(msg):
    print(msg)

x = log("Hi")
print(x)`}
/>

No `return` → caller gets **`None`**.

## Real use case: check with is None

<PythonExample
  filename="is_none.py"
  code={`value = None

if value is None:
print("No value set")

def find_user(id): # pretend not found
return None

user = find_user(99)
if user is None:
print("User not found")`}
/>

Use **`is None`** — not `==` — when testing for missing values.

## .get() returns None for missing keys

<PythonExample
  filename="get_none.py"
  code={`data = {"name": "Ada"}
email = data.get("email")
print(email)
print(email is None)`}
/>

<Callout variant="warning">
  Do not confuse `None` with `0`, `False`, or `""`. They are all “falsy” in `if` tests,
  but they mean different things.
</Callout>

<Callout variant="tip">
  Use `None` as a default in functions: `def f(x=None):` then set `x = []` inside if
  needed — never use `def f(x=[]):`.
</Callout>

## Recap

- **`None`** — missing or empty value
- **No return** → function result is `None`
- **`is None`** — preferred check; `.get()` uses `None` for missing keys
