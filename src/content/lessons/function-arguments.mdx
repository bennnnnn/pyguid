---
title: "Function arguments"
description: "Python function arguments tutorial: parameters, arguments, and calling with values. Beginner examples explain terminology and typical calling conventions."
order: 98
chapter: 14
chapterTitle: "Functions"
level: beginner
quickAnswer: "Parameters are names in the def line; arguments are the values you pass when you call the function, like greet('Ada')."
faqs:
  - question: "What is the difference between a parameter and an argument?"
    answer: "Parameters appear in the function definition. Arguments are the actual values supplied in the call."
  - question: "How many arguments can a Python function have?"
    answer: "As many as you need, separated by commas in def and in the call. Very long lists are a sign to use a dict or dataclass instead."
  - question: "Must arguments match the parameter order?"
    answer: "For positional arguments, yes—the first argument fills the first parameter unless you use keyword arguments."
  - question: "Can I call a function with no arguments?"
    answer: "Yes, if the definition has no required parameters or only parameters with defaults."
---

## Why this matters

Arguments make functions flexible—the same add() works for any numbers, the same format_name() for any string. They lead to default args, \*args, and keyword patterns used everywhere in the standard library. Clear parameter names make code readable for you and teammates.

Ordering coffee: “latte” uses defaults (regular milk, medium), but you can say “oat milk, large.” **Arguments** let callers skip details or name exactly what they want.

## One new idea: default parameter values

<PythonExample
  filename="default.py"
  code={`def greet(name, greeting="Hello"):
    print(f"{greeting}, {name}")

greet("Ada")
greet("Ada", "Hi")`}
/>

**Line by line**

- `greeting="Hello"` — used when the caller skips that argument
- `greet("Ada")` — only `name` is required
- Defaults must come **after** parameters without defaults

## Keyword arguments

Pass values by **parameter name** — order does not matter:

<PythonExample
  filename="keyword.py"
  code={`def describe(pet, age, color):
    print(f"{pet}, age {age}, color {color}")

describe(age=3, pet="cat", color="black")`}
/>

## Mix position and keyword

Position first, then keywords:

<PythonExample
  filename="mix.py"
  code={`def power(base, exponent=2):
    return base ** exponent

print(power(3))
print(power(3, 3))
print(power(base=2, exponent=5))`}
/>

## Real use case: flexible totals

<PythonExample
  filename="args.py"
  code={`def total(*numbers):
    sum = 0
    for n in numbers:
        sum += n
    return sum

print(total(1, 2, 3))
print(total(10, 20, 5, 5))`}
/>

`*numbers` collects any number of positional values into a tuple.

## Many keyword values with \*\*kwargs

<PythonExample
  filename="kwargs.py"
  code={`def show_info(**info):
    for key, value in info.items():
        print(key, value)

show_info(name="Ada", role="dev")`}
/>

<Callout variant="warning">
  Mutable defaults (like `def f(items=[]):`) are a common trap. Use `None` and create the
  list inside the function when you learn advanced patterns.
</Callout>

<Callout variant="tip">
  For beginners, stick to simple parameters and one or two defaults. `*args` and
  `**kwargs` help when building flexible APIs.
</Callout>

<TryIt title="Try it yourself" answer="Hello, Alex">
  Define `def greet(name, greeting="Hello"):` and call `greet("Alex")`.
</TryIt>

## Recap

- **Defaults** — optional arguments with `=`
- **Keyword args** — `name=value`, any order
- **`*args` / `**kwargs`\*\* — many values at once

Next: **Return values** — send results back from a function.
