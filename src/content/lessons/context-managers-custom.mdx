---
title: "Custom context managers"
description: "Use with and contextlib to guarantee setup and cleanup Learn Custom context managers in this Files lesson with runnable Python examples on PyGuide...."
order: 119
chapter: 17
chapterTitle: "Files"
level: beginner
quickAnswer: "Use with and contextlib to guarantee setup and cleanup. This PyGuide lesson shows Custom context managers with examples you can run in the browser."
---

## Why this matters

Custom context managers shows up throughout Files and later chapters. Understanding it here makes the next lessons easier and helps you read real Python code with confidence. PyGuide keeps each page focused on one idea so you can practice without getting lost.


Opening a file with **`with`** already guarantees the file **closes** even if an error happens. You can write the same pattern for **timers**, **locks**, or **temporary settings** using **`contextlib`**.

## Files already use with

```python
with open("notes.txt", encoding="utf-8") as f:
    text = f.read()
# file closed here automatically
```

## One new idea: contextlib.contextmanager

<PythonExample
  filename="context_demo.py"
  code={`from contextlib import contextmanager

@contextmanager
def tag(name):
print(f"[{name}] start")
try:
yield name
finally:
print(f"[{name}] end")

with tag("job") as label:
print("Working inside", label)`}
/>

**Line by line**

- Code **before** `yield` — setup
- **`yield`** — body of `with` block runs
- Code **after** `yield` in `finally` — cleanup always runs

## Class-based style (preview)

Classes can implement `__enter__` and `__exit__` — same idea, more boilerplate. `contextmanager` is the quick version.

<Callout variant="tip">
  Use context managers whenever you pair **open/close**, **acquire/release**, or
  **start/stop**.
</Callout>

## Recap

- **`with`** guarantees cleanup
- **`@contextmanager`** + **`yield`** for custom blocks
- **`finally`** runs even on errors
