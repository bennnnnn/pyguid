---
title: "super() in inheritance"
description: "Call parent class methods with super() Learn super() in inheritance in this Object-Oriented Python lesson with runnable Python examples on PyGuide...."
order: 187
chapter: 37
chapterTitle: "Inheritance"
section: "super()"
part: 8
partTitle: "Object-oriented Python"
level: beginner
quickAnswer: "super() in inheritance in Python — this lesson explains the idea with short examples and common beginner mistakes to avoid."
---

## Why this matters

super() in inheritance shows up throughout Object-Oriented Python and later chapters. Understanding it here makes the next lessons easier and helps you read real Python code with confidence. PyGuide keeps each page focused on one idea so you can practice without getting lost.

A child class can **extend** a parent without rewriting everything. **`super()`** calls the **parent’s** version of a method — often **`__init__`** first.

## One new idea: super().**init**()

<PythonExample
  filename="super_init.py"
  code={`class Animal:
    def __init__(self, name):
        self.name = name

class Dog(Animal):
def **init**(self, name, breed):
super().**init**(name)
self.breed = breed

    def describe(self):
        return f"{self.name} is a {self.breed}"

d = Dog("Buddy", "lab")
print(d.describe())`}
/>

**Line by line**

- `Dog` inherits from `Animal`
- `super().__init__(name)` runs parent setup
- `self.breed` is **Dog-specific**

## Override but reuse parent

<PythonExample
  filename="override.py"
  code={`class Greeter:
    def greet(self):
        return "Hello"

class LoudGreeter(Greeter):
def greet(self):
base = super().greet()
return base + "!!!"

print(LoudGreeter().greet())`}
/>

## Recap

- **`super()`** — parent class methods
- Common in **`__init__`** and overridden methods
- Keeps parent logic in **one place**
