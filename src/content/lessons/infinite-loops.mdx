---
title: "Infinite loops"
description: "Use while True with break to repeat until a condition Learn Infinite loops in this Loops lesson with runnable Python examples on PyGuide. Beginner-friendly..."
order: 103
chapter: 15
chapterTitle: "Loops"
part: 5
partTitle: "Collections and loops"
level: beginner
quickAnswer: "Use while True with break to repeat until a condition. This PyGuide lesson shows Infinite loops with examples you can run in the browser."
---

## Why this matters

Infinite loops shows up throughout Loops and later chapters. Understanding it here makes the next lessons easier and helps you read real Python code with confidence. PyGuide keeps each page focused on one idea so you can practice without getting lost.

A game loop runs forever until you press quit. **`while True:`** means “keep going” — you **must** have a **`break`** or the program never stops (on your PC, Ctrl+C interrupts).

## One new idea: while True + break

<PythonExample
  filename="simulated.py"
  code={`# Simulated menu — not a real infinite loop in browser
choices = ["add", "add", "quit"]
for action in choices:
    print("Action:", action)
    if action == "quit":
        print("Goodbye")
        break`}
/>

On your computer:

```python
while True:
    cmd = input("Command (quit to exit): ")
    if cmd == "quit":
        break
    print("You typed:", cmd)
```

## Danger: no break

```python
while True:
    print("Forever")  # never stops — avoid this
```

<Callout variant="danger" title="Oops — Python is yelling">
  Forgot **`break`** → infinite output or frozen program. Always plan the **exit**
  condition first.
</Callout>

## Recap

- **`while True`** — loop until **`break`**
- Menus and games use this pattern
- Always know how to **exit**
