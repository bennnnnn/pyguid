---
title: "Mini project: Guessing game"
description: "Build a number guessing game with while loops and break Learn Mini project: Guessing game in this Loops lesson with runnable Python examples on PyGuide...."
order: 56
chapter: 9
chapterTitle: "Loops"
level: beginner
quickAnswer: "Build a number guessing game with while loops and break. This PyGuide lesson shows Mini project: Guessing game with examples you can run in the browser."
---

## Why this matters

Mini project: Guessing game shows up throughout Loops and later chapters. Understanding it here makes the next lessons easier and helps you read real Python code with confidence. PyGuide keeps each page focused on one idea so you can practice without getting lost.


Pick a secret number. The player guesses until they get it right — classic **`while`** + **`if`** + **`break`** practice.

## Simulated version (browser-safe)

<PythonExample
  filename="guess.py"
  code={`secret = 7
guesses = [3, 9, 7]

for guess in guesses:
print("You guessed:", guess)
if guess == secret:
print("Correct!")
break
elif guess < secret:
print("Too low")
else:
print("Too high")`}
/>

On your computer, replace the list with **`while True:`** and **`int(input(...))`**.

## Full while loop pattern (local Python)

```python
secret = 7
while True:
    guess = int(input("Guess: "))
    if guess == secret:
        print("You win!")
        break
    elif guess < secret:
        print("Too low")
    else:
        print("Too high")
```

## Stretch goals

- Count guesses with a counter
- Limit to 5 tries, then `break` with "Game over"
- Use `random.randint(1, 10)` from `import random` (local)

## Recap

- **`while`** repeats until **`break`**
- Compare guess to secret with **`==`**, **`<`**, **`>`**
