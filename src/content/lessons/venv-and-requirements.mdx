---
title: "Virtual environments and requirements.txt"
description: "Python venv and requirements.txt for beginners: isolate dependencies, install packages with pip, and reproduce environments on any machine easily."
order: 134
chapter: 19
chapterTitle: "Packages and Imports"
level: beginner
quickAnswer: "Create a venv with python -m venv .venv, activate it, then pip install -r requirements.txt to install pinned dependencies."
faqs:
  - question: "What is a Python virtual environment?"
    answer: "An isolated Python installation and site-packages folder for one project, so dependencies do not clash with other projects."
  - question: "How do I create requirements.txt?"
    answer: "After installing packages in the venv, run pip freeze > requirements.txt to list exact versions."
  - question: "Do I commit the venv folder to git?"
    answer: "Usually no. Commit requirements.txt and let others recreate the venv locally."
  - question: "Why use requirements.txt instead of installing manually?"
    answer: "It records exact versions so everyone and CI install the same dependencies reproducibly."
---

## Why this matters

Without virtual environments, global pip installs break other projects and tutorials. requirements.txt documents what your app needs so teammates and deploys match your machine. This is standard practice before Flask, data science, or any third-party library work.

Your project gets its own **sandbox** so package versions do not fight other projects. A **virtual environment (`venv`)** is that sandbox; **`requirements.txt`** lists what to install.

## One new idea: venv per project

On your computer (terminal):

```bash
python -m venv .venv
source .venv/bin/activate   # Mac/Linux
# .venv\Scripts\activate    # Windows
pip install requests
pip freeze > requirements.txt
```

| Step               | Why                            |
| ------------------ | ------------------------------ |
| `python -m venv`   | create isolated Python         |
| `activate`         | use that Python for `pip`      |
| `requirements.txt` | share exact packages with team |

## requirements.txt example

```text
requests==2.31.0
```

Teammates run:

```bash
pip install -r requirements.txt
```

<Callout variant="note">
  PyGuide lessons do not run `pip` in the browser. Learn the commands locally when you
  start real projects.
</Callout>

<TryIt title="Try it yourself" answer="(conceptual)">
  In your own words: why is `venv` safer than installing every package globally?
</TryIt>

## Recap

- **`venv`** — isolated environment per project
- **`pip freeze > requirements.txt`** — save dependencies
- **`pip install -r requirements.txt`** — restore them

Next: **`if __name__ == "__main__"`** — runnable modules.
