---
title: "The type() function"
description: "Python type() and isinstance() inspect object types at runtime. Beginner guide explains when to check types, basic introspection, and readable alternatives."
order: 18
chapter: 3
chapterTitle: "Data Types"
level: beginner
quickAnswer: "Call type(x) to see an object's type, or isinstance(x, int) to test whether it is an int or a subclass without brittle exact-type checks."
faqs:
  - question: "What does type() return in Python?"
    answer: "type(obj) returns the class of the object, such as <class 'int'> or <class 'str'>."
  - question: "Should I use type(x) == int or isinstance(x, int)?"
    answer: "Prefer isinstance for checks. It respects inheritance and is the conventional choice in library code."
  - question: "Can type() create classes dynamically?"
    answer: "Advanced use yes, but beginners mainly use type() for inspection and debugging, not for defining new types."
  - question: "How do I check if a variable is a string?"
    answer: "Use isinstance(x, str). Also remember that str is the text type; bytes is separate for raw binary data."
---

## Why this matters

Type checks guard functions that expect numbers or strings and help debug unexpected API data. Overusing type() hurts flexibility; isinstance() is usually better. Knowing types connects to int(), str(), and validation patterns in real scripts.

Four kinds of luggage tags: **number**, **decimal**, **text**, **yes/no**. **`type()`** reads the tag when you are not sure what you are holding.

## The four core types — recap

| Type  | Example   | `type()` shows    |
| ----- | --------- | ----------------- |
| int   | `42`      | `<class 'int'>`   |
| float | `3.14`    | `<class 'float'>` |
| str   | `"hello"` | `<class 'str'>`   |
| bool  | `True`    | `<class 'bool'>`  |

<PythonExample
  filename="recap.py"
  code={`values = [42, 3.14, "hello", True]
for v in values:
    print(v, "->", type(v))`}
/>

## One new idea: type(value)

<PythonExample
  filename="types.py"
  code={`x = 42
print(type(x))

name = "Ada"
print(type(name))`}
/>

Use **`type()`** while learning whenever output surprises you.

## Division always makes float

<PythonExample
  filename="after_math.py"
  code={`result = 8 / 2
print(result)
print(type(result))`}
/>

Looks like `4` — but type is **`float`** (`4.0`).

## Convert types when you must

### str() — number to text for `+`

<PythonExample
  filename="str_convert.py"
  code={`age = 25
message = "You are " + str(age)
print(message)`}
/>

Without **`str(age)`**, **`"You are " + age`** → **TypeError**.

### int() and float() — text to numbers

<PythonExample
  filename="int_convert.py"
  code={`text = "42"
number = int(text)
print(number + 8)

price_text = "9.99"
price = float(price_text)
print(price + 1)`}
/>

<Callout variant="danger" title="Oops — Python is yelling">
  **`int("hello")`** → **ValueError**. The text must look like a real number.
</Callout>

## Same look, different type

<PythonExample
  filename="look_alike.py"
  code={`print(42, type(42))
print("42", type("42"))`}
/>

| Value  | Type  |
| ------ | ----- |
| `42`   | `int` |
| `"42"` | `str` |

<TryIt title="Try it yourself" answer="15">
  With `text = "10"`, set `n = int(text)` and print `n + 5`.
</TryIt>

## Chapter 3 mini project: profile card

<PythonExample
  filename="profile.py"
  code={
    '# Mini project: types working together\nname = "Ada"\nage = 36\nheight_m = 1.68\nis_student = False\n\nprint("=== Profile ===")\nprint("Name:", name, "| type:", type(name).__name__)\nprint("Age:", age, "| type:", type(age).__name__)\nprint("Height:", height_m, "| type:", type(height_m).__name__)\nprint("Student:", is_student)\n\nnext_year = age + 1\nprint("Next year you will be", next_year)'
  }
/>

Change the values. Run. Notice which lines need quotes and which do not.

## Recap

- **`type(x)`** — what kind of value
- **`str` / `int` / `float`** — convert when mixing types
- **`+`** on strings needs **`str()`** for numbers

You finished **Data Types**. Next: **Comparisons** — `==`, `!=`, `<`, and friends.
