---
title: "append() vs extend() in Python"
description: "Python append vs extend: when to add one item versus merge an iterable into a list. Side-by-side beginner examples that fix nested list bugs fast."
order: 60
chapter: 10
chapterTitle: "Lists"
level: beginner
quickAnswer: "Use append() to add one element; use extend() to add each element from an iterable. append([1,2]) nests a list; extend([1,2]) adds 1 and 2."
faqs:
  - question: "Should I use append or extend to combine two lists?"
    answer: "Use extend(other_list) or a slice assignment if you want all elements merged. append(other_list) adds the second list as one nested element."
  - question: "What happens if I append a list to a list?"
    answer: "You get a list containing another list as a single item, for example [1, 2, [3, 4]], not [1, 2, 3, 4]."
  - question: "Is list += the same as extend?"
    answer: "For lists, += with another list behaves like extend for standard list objects, adding elements in place. Still know append vs extend for clarity when reading code."
  - question: "Which is faster, append or extend?"
    answer: "For many items, extend is clearer and avoids repeated function calls in a loop. For one item, append is correct. Readability and correct structure matter more than micro-speed here."
---

## Why this matters

This pair causes some of the most common list bugs in first projects—unexpected [[...]] structures and wrong counts. Choosing the right method keeps data flat and predictable. Once you internalize the rule, list-building in loops feels straightforward.

**Short answer:** `append()` adds **one** item (any type). `extend()` adds **many** items by copying elements from another iterable.

## append() — one item at the end

<PythonExample
  filename="append_one.py"
  code={`items = ["a", "b"]
items.append("c")
print(items)`}
/>

## extend() — merge another iterable

<PythonExample
  filename="extend_many.py"
  code={`items = ["a", "b"]
items.extend(["c", "d"])
print(items)`}
/>

## Side-by-side

|                     | `append(x)`                     | `extend(iterable)`             |
| ------------------- | ------------------------------- | ------------------------------ |
| Adds                | **one** object `x`              | **each element** from iterable |
| `append(["x","y"])` | list becomes `[..., ["x","y"]]` | —                              |
| `extend(["x","y"])` | —                               | list becomes `[..., "x", "y"]` |
| Returns             | `None`                          | `None`                         |
| Changes original    | yes                             | yes                            |

## Common mistake: nested list with append

<PythonExample
  filename="nested.py"
  code={`row = [1, 2]
grid = []
grid.append(row)
row.append(3)
print(grid)`}
/>

`append(row)` stores **one** list object — aliasing bites later. Use `extend` or `copy` when building grids.

<TryIt title="Try it yourself" answer='["a", "b", "c", "d"]'>
  Start `items = ["a", "b"]`. Use `extend(["c", "d"])`. Print `items`.
</TryIt>

## Recap

- **append** — single item
- **extend** — unpack an iterable into the list
- Both return **None**; both modify in place

Next: **list extend()** in depth.
