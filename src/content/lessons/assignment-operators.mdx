---
title: "Assignment operators"
description: "Python assignment operators: =, +=, -=, *=, /=, and related in-place updates. Beginner tutorial shows shorthand that modifies variables and mutating types."
order: 21
chapter: 6
chapterTitle: "Numbers and Operators"
level: beginner
quickAnswer: "Assignment operators combine assignment with an operation, such as x += 1 meaning x = x + 1, often updating the variable in place for mutable objects."
faqs:
  - question: "What does += do in Python?"
    answer: "x += y evaluates x + y and binds the result back to x, equivalent to x = x + y for numbers and many types."
  - question: "Does list += add one element or many?"
    answer: "For lists, += with an iterable extends the list with each element, similar to extend(), not a single nested append."
  - question: "Are assignment operators the same as math operators?"
    answer: "They combine math (or string/list ops) with assignment. //=, %=, **=, and others follow the same pattern."
  - question: "Can I use assignment operators on strings?"
    answer: "Yes. s += '!' appends text because strings support concatenation with +, creating a new string object."
---

## Why this matters

+= and friends appear in every counter, accumulator, and loop. For lists, += behaves like extend, which surprises people expecting append. Clear assignment shorthand makes loops readable and matches how C-style languages express updates.

You already know **`score = score + 10`**. **`+=`** is the shorthand: “add to what is already in the box.”

## One new idea: += and friends

<PythonExample
  filename="plus_eq.py"
  code={`points = 10
points += 5
print(points)`}
/>

| Long form             | Shortcut      |
| --------------------- | ------------- |
| `points = points + 5` | `points += 5` |
| `x = x - 1`           | `x -= 1`      |
| `n = n * 2`           | `n *= 2`      |
| `n = n / 2`           | `n /= 2`      |

## Real use case: game score

<PythonExample
  filename="game.py"
  code={`score = 0
score += 10
score += 25
score += 3
print("Final score:", score)`}
/>

<Callout variant="danger" title="Oops — Python is yelling">
  **`+=`** is **not** the same as **`==`**. **`+=`** updates a variable; **`==`** compares
  two values.
</Callout>

<TryIt title="Try it yourself" answer="8">
  Set `x = 10`, then `x -= 2`, then print `x`.
</TryIt>

## Recap

- **`+=` `-=` `*=` `/=`** update in place
- Same meaning as the long `x = x + ...` form

Next: **Comparison operators** — `==`, `<`, `>=`, and more.
