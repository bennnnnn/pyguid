---
title: "copy and deepcopy"
description: "Clone objects without accidental shared nested lists Learn copy and deepcopy in this Python Standard Library lesson with runnable Python examples on..."
order: 187
chapter: 54
chapterTitle: "Memory and Object Model"
section: "Deep copy"
part: 10
partTitle: "Debugging, testing, and clean code"
level: beginner
quickAnswer: "Clone objects without accidental shared nested lists. This PyGuide lesson shows copy and deepcopy with examples you can run in the browser."
---

## Why this matters

copy and deepcopy shows up throughout Python Standard Library and later chapters. Understanding it here makes the next lessons easier and helps you read real Python code with confidence. PyGuide keeps each page focused on one idea so you can practice without getting lost.

`b = a` makes two names point at the **same** list. **`copy.copy`** makes a new outer list, but nested lists may still be shared. **`copy.deepcopy`** clones everything inside.

## One new idea: shallow vs deep

<PythonExample
  filename="copy_demo.py"
  code={`import copy

original = [[1, 2], [3, 4]]
shallow = copy.copy(original)
deep = copy.deepcopy(original)

original[0].append(99)

print("original:", original)
print("shallow:", shallow)
print("deep:", deep)`}
/>

**shallow** inner lists still alias **original**. **deep** is fully independent.

| Assignment / copy  | Nested lists shared? |
| ------------------ | -------------------- |
| `b = a`            | yes (same object)    |
| `copy.copy(a)`     | maybe                |
| `copy.deepcopy(a)` | no                   |

<Callout variant="tip">
  You saw similar issues with **list copy** and **mutable class attributes** — same
  underlying idea.
</Callout>

## Recap

- **`import copy`**
- **Shallow** — outer container only
- **Deep** — full tree duplicated
