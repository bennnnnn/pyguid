---
title: "join()"
description: "Combine a list of strings with str.join() Learn join in this Strings lesson with runnable Python examples on PyGuide. Beginner-friendly explanations — no..."
order: 28
chapter: 7
chapterTitle: "Strings"
level: beginner
quickAnswer: "Combine a list of strings with str.join(). This PyGuide lesson shows join with examples you can run in the browser."
---

## Why this matters

join shows up throughout Strings and later chapters. Understanding it here makes the next lessons easier and helps you read real Python code with confidence. PyGuide keeps each page focused on one idea so you can practice without getting lost.


**`split()`** cuts a string apart. **`join()`** is the glue: you pick the separator, pass a list of strings, and get one combined string back.

## One new idea: separator.join(list)

<PythonExample
  filename="join.py"
  code={`words = ["Python", "is", "great"]
sentence = " ".join(words)
print(sentence)`}
/>

**Line by line**

- `" "` — the **glue** (space between words)
- `.join(words)` — stick `words` together
- Result: `"Python is great"`

Read it as: **“space-join these words.”**

## Different separators

<PythonExample
  filename="separators.py"
  code={`items = ["2024", "05", "23"]
print("-".join(items))
print(", ".join(["a", "b", "c"]))`}
/>

## Round trip with split()

<PythonExample
  filename="roundtrip.py"
  code={`original = "one,two,three"
parts = original.split(",")
restored = ",".join(parts)
print(restored)`}
/>

## Common mistake: numbers in the list

<Callout variant="danger" title="Oops — Python is yelling">
  **`"-".join([1, 2, 3])`** → **TypeError**. Every item must be a **string**. Convert
  first: **`"-".join(str(n) for n in nums)`** (pattern shown below).
</Callout>

<PythonExample
  filename="str_first.py"
  code={`nums = [1, 2, 3]
text = "-".join(str(n) for n in nums)
print(text)`}
/>

## Recap

- Call **`join`** on the **separator** string
- All items must be **`str`**
- Pairs naturally with **`split()`**
