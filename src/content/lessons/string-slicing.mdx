---
title: "String slicing"
description: "Python string slicing extracts substrings with [start:stop:step] just like lists. Beginner tutorial on indexing, reversing text, and immutability reminders."
order: 32
chapter: 3
chapterTitle: "Data Types"
section: "Strings"
level: beginner
quickAnswer: "Use text[start:stop] to slice a substring; text[0:3] gives the first three characters and text[-1] gives the last character."
faqs:
  - question: "How do I get the first three characters of a string?"
    answer: "Use text[:3] or text[0:3]. Both return a new string with the first three characters."
  - question: "Can I change a character with string slicing?"
    answer: "No. Strings are immutable. Build a new string by concatenating slices and replacement text instead."
  - question: "How do I reverse a string in Python?"
    answer: "Use text[::-1] with a step of -1, or reversed(text) inside join for another idiomatic approach."
  - question: "Are string slices inclusive of the stop index?"
    answer: "No. The stop index is excluded, same as list slicing: 'Python'[0:3] is 'Pyt'."
---

## Why this matters

String slicing powers prefixes, extensions, and parsing fixed-width fields without regex. Because strings are immutable, slices always produce new text. Mastering slice notation transfers directly to lists and bytes.

A word on a strip of paper: you can read the **whole** word or highlight **letters 2–4**. **Slicing** `text[start:end]` highlights a piece — **`end` is not included**.

## One new idea: [start:end]

<PythonExample
  filename="slice.py"
  code={`text = "Python"
print(text[0:3])
print(text[2:])
print(text[:4])`}
/>

| Slice   | Result   | Meaning         |
| ------- | -------- | --------------- |
| `[0:3]` | `"Pyt"`  | index 0,1,2     |
| `[2:]`  | `"thon"` | from index 2 on |
| `[:4]`  | `"Pyth"` | up to, not 4    |

Same rules as list slicing (you will use these everywhere).

## Negative indexes

<PythonExample
  filename="neg_slice.py"
  code={`text = "Programming"
print(text[-3:])
print(text[:-1])`}
/>

| Slice   | Result       |
| ------- | ------------ |
| `[-3:]` | last 3 chars |
| `[:-1]` | all but last |

## Step: every second letter, reverse

<PythonExample
  filename="step.py"
  code={`text = "abcdef"
print(text[::2])
print(text[::-1])`}
/>

| Slice    | Effect              |
| -------- | ------------------- |
| `[::2]`  | every 2nd character |
| `[::-1]` | reversed copy       |

## Real use case: hide part of an email

<PythonExample
  filename="mask.py"
  code={`email = "ada@example.com"
name = email[:3]
domain = email[email.find("@"):]
print(name + "***" + domain)`}
/>

<TryIt title="Try it yourself" answer="Py">
  With `s = "Python"`, print `s[0:2]`.
</TryIt>

## Recap

- **`[start:end]`** — `end` excluded
- **`[:]`** — whole copy
- **`[::-1]`** — reversed copy

Next chapter topic: **Booleans** — `True` and `False`.
