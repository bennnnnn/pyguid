---
title: "The print() function"
description: "Python print() function tutorial for beginners: display text, multiple values, sep and end options, and your first hello world with runnable PyGuide examples."
order: 3
chapter: 2
chapterTitle: "Running Python Code"
section: "The print function"
part: 1
partTitle: "Absolute beginner"
level: beginner
quickAnswer: "Call print(value) to write text to the console; separate multiple arguments with commas and print adds a newline by default."
---

## Why this matters

print() is the fastest feedback loop—see results immediately while learning syntax. It stays useful for debugging even after you learn return and logging. Hello world is small, but it proves your environment works and builds confidence.

A **`print()`** is like a megaphone for your program: whatever you put inside gets shown on the screen. It is the fastest way to see if your code is doing what you expect.

## One new idea: show text with quotes

<PythonExample filename="hello.py" code={`print("Hello, Python!")`} />

**Line by line**

- `print` — the function name
- `("Hello, Python!")` — the **argument**: what to display
- Double `"` or single `'` quotes mean **text** (a string)

<PythonExample filename="quotes.py" code={`print('Single quotes work too')`} />

## Show numbers (no quotes)

<PythonExample
  filename="numbers.py"
  code={`print(42)
print(3.14)`}
/>

| You write     | Python treats it as |
| ------------- | ------------------- |
| `print(42)`   | number **42**       |
| `print("42")` | text **"42"**       |

<Callout variant="note">
  Using **+**, **-**, **\***, and **/** on numbers comes later in the **Operators** lessons. For
  now, print whole numbers as literals like `42` or `3.14`.
</Callout>

## Several values: commas add spaces

<PythonExample
  filename="multiple.py"
  code={`print("The answer is", 42)
print("Score", 10)`}
/>

**Output**

```text
The answer is 42
Score 10
```

## Blank line

<PythonExample
  filename="blank.py"
  code={`print("Line 1")
print()
print("Line 3")`}
/>

`print()` with nothing inside prints an empty line.

## Control spacing: `sep` and `end`

<PythonExample
  filename="sep_end.py"
  code={`print("apple", "banana", "cherry", sep=", ")
print("Loading", end="...")
print(" done")`}
/>

| Option | Default  | Example effect          |
| ------ | -------- | ----------------------- |
| `sep`  | space    | `sep=", "` → commas     |
| `end`  | new line | `end="..."` → same line |

<Callout variant="note">
  For **new lines and quotes inside text** (`\n`, `\"`, and more), see the next lesson:
  **Escape characters**.
</Callout>

<Callout variant="danger" title="Oops — Python is yelling">
  **`print(hello)`** without quotes looks for a variable named `hello`. You have not
  created one yet → **NameError**. For now, put words in **quotes**.
</Callout>

## Recap

- **`print("text")`** — show strings
- **`print(42)`** — show number literals (no quotes)
- **Commas** — several items with spaces between
- **Quotes** = text; **no quotes** = number
- **Variables** in `print()` come after the Variables lessons
