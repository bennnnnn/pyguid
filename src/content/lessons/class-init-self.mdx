---
title: "__init__ and self"
description: "Initialize objects with __init__ and the self parameter Learn __init__ and self in this Object-Oriented Python lesson with runnable Python examples on..."
order: 183
chapter: 35
chapterTitle: "Classes and Objects"
section: "__init__ and self"
part: 8
partTitle: "Object-oriented Python"
level: beginner
quickAnswer: "Initialize objects with __init__ and the self parameter. This PyGuide lesson shows __init__ and self with examples you can run in the browser."
---

## Why this matters

**init** and self shows up throughout Object-Oriented Python and later chapters. Understanding it here makes the next lessons easier and helps you read real Python code with confidence. PyGuide keeps each page focused on one idea so you can practice without getting lost.

A cookie cutter shapes each cookie the same way but each cookie has **its own** sprinkles. **`__init__`** sets up **each object**; **`self`** means “this particular object.”

## One new idea: def **init**(self, ...)

<PythonExample
  filename="init.py"
  code={`class Dog:
    def __init__(self, name):
        self.name = name

    def speak(self):
        print(self.name, "says woof")

buddy = Dog("Buddy")
luna = Dog("Luna")
buddy.speak()
luna.speak()`}
/>

**Line by line**

- `__init__` runs **automatically** when you call `Dog("Buddy")`
- `self` — the new object being built
- `self.name = name` — store data **on** the object

## Why self?

<PythonExample
  filename="two_dogs.py"
  code={`class Dog:
    def __init__(self, name):
        self.name = name

a = Dog("Max")
b = Dog("Zoe")
print(a.name, b.name)`}
/>

`a.name` and `b.name` are **separate** — each `self` was different during `__init__`.

<Callout variant="warning" title="Oops — Python is yelling">
  Forgetting **`self`** in a method: **`def speak():`** inside a class → **`TypeError`**
  when called. Instance methods need **`self`** as the first parameter.
</Callout>

## Recap

- **`__init__`** — setup for each object
- **`self`** — this instance inside methods
- **`self.attribute`** — data stored on the object
