---
title: "pytest basics"
description: "pytest basics for Python beginners: write test functions, run pytest, and assert outcomes. Covers discovery, reading failures, and simple test file layout."
order: 214
chapter: 50
chapterTitle: "Testing"
part: 10
partTitle: "Debugging, testing, and clean code"
level: beginner
quickAnswer: "Name test files test_*.py, write functions starting with test_, and run pytest in the terminal; pytest reports failures with helpful assert output."
---

## Why this matters

Automated tests catch regressions before users do and document expected behavior. pytest's plain assert syntax lowers the barrier compared to heavyweight frameworks. Even a few tests on core functions build confidence when refactoring beginner projects.

**assert** checks one idea at a time. **pytest** runs **many** checks from files named `test_*.py` and reports pass/fail clearly — how most Python teams test code.

## One new idea: test functions

```python
# test_slugify.py
from myapp import slugify

def test_lowercase():
    assert slugify("Hello") == "hello"

def test_spaces():
    assert slugify("a b") == "a-b"
```

Rules pytest expects:

- File name starts with **`test_`**
- Function name starts with **`test_`**
- Body uses plain **`assert`**

## Run locally

```bash
pip install pytest
pytest
```

Pytest discovers tests automatically and shows green/red output.

## Browser-safe demo

<PythonExample
  filename="test_demo.py"
  code={`def double(n):
    return n * 2

def test_double_even():
assert double(2) == 4

def test_double_zero():
assert double(0) == 0

# Manual run without pytest

test_double_even()
test_double_zero()
print("All tests passed")`}
/>

On your PC, pytest calls those functions for you and stops on the first failure.

<Callout variant="note">
  Install pytest in a **venv** (see **venv and requirements**). Skulpt does not run
  pytest.
</Callout>

## Recap

- **`test_*.py`** files with **`test_*` functions**
- **`pytest`** command runs the suite
- Builds on **Testing with assert**
