---
title: "from, import, and as"
description: "Import names directly and rename them to avoid clashes Learn from, import, and as in this Modules lesson with runnable Python examples on PyGuide...."
order: 130
chapter: 18
chapterTitle: "Modules"
level: beginner
quickAnswer: "Import names directly and rename them to avoid clashes. This PyGuide lesson shows from, import, and as with examples you can run in the browser."
---

## Why this matters

from, import, and as shows up throughout Modules and later chapters. Understanding it here makes the next lessons easier and helps you read real Python code with confidence. PyGuide keeps each page focused on one idea so you can practice without getting lost.


You do not always want `math.sqrt` — sometimes you want **`sqrt`** alone. **`from module import name`** pulls one tool onto your workbench; **`as`** gives it a nickname.

## One new idea: from ... import

<PythonExample
  filename="from_import.py"
  code={`from math import sqrt, pi

print(sqrt(16))
print(round(pi, 2))`}
/>

Only **`sqrt`** and **`pi`** enter your namespace — not the whole `math` module.

## Rename with as

<PythonExample
  filename="as_alias.py"
  code={`import json as js

data = js.loads('{"ok": true}')
print(data)`}
/>

Use **`as`** when the name is long or clashes with yours (`import datetime as dt`).

## Styles compared

| Statement                              | Effect                   |
| -------------------------------------- | ------------------------ |
| `import math`                          | module as `math`         |
| `from math import sqrt`                | function `sqrt` directly |
| `import json as js`                    | module nickname `js`     |
| `from collections import Counter as C` | short name `C`           |

<Callout variant="warning">
  `from module import *` dumps **everything** into your namespace — hard to debug. Avoid
  in real projects.
</Callout>

## Recap

- **`from m import x`** — one name
- **`import m as alias`** — shorten module path
- Prefer explicit imports over `import *`
