---
title: "Capstone: Personality quiz"
description: "Build a scored quiz with dicts, if/elif, and print formatting Learn Capstone: Personality quiz in this Python Mini Programs lesson with runnable Python..."
order: 184
chapter: 52
chapterTitle: "Clean Python Style"
section: "Mini programs"
part: 10
partTitle: "Debugging, testing, and clean code"
level: beginner
quickAnswer: "Build a scored quiz with dicts, if/elif, and print formatting. This PyGuide lesson shows Capstone: Personality quiz with examples you can run in the browser."
---

## Why this matters

Capstone: Personality quiz shows up throughout Python Mini Programs and later chapters. Understanding it here makes the next lessons easier and helps you read real Python code with confidence. PyGuide keeps each page focused on one idea so you can practice without getting lost.

Buzzfeed-style quizzes map answers to a **type**. You will score choices with a **dict**, branch with **`if`/`elif`**, and print a result — the same pattern as the **Quiz app**, but you pick the theme.

## Questions and scoring

<PythonExample
  filename="quiz.py"
  code={`scores = {"explorer": 0, "builder": 0, "helper": 0}

def answer(choice):
scores[choice] = scores.get(choice, 0) + 1

# Simulated answers

answer("explorer")
answer("builder")
answer("explorer")

winner = max(scores, key=scores.get)
print("Your type:", winner)
print("Breakdown:", scores)`}
/>

**Line by line**

- **`scores` dict** — tallies per personality
- **`answer`** bumps one key
- **`max(..., key=scores.get)`** — highest score wins ties arbitrarily

## Add questions (local)

```python
print("1) Adventure  2) Build  3) Help friends")
choice = input("> ")
if choice == "1":
    answer("explorer")
elif choice == "2":
    answer("builder")
else:
    answer("helper")
```

Repeat for 3–5 questions, then print the winner.

## Stretch goals

- Store questions in a **list of dicts**
- Save results to **JSON** (see **JSON todo**)
- Use **enum** for result types

## Recap

- **Dict** for scores, **max** for winner
- Combines **if**, **input**, and presentation skills
- Customize names and questions for your own quiz site idea
