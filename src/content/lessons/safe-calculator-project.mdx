---
title: "Mini project: Safe calculator"
description: "Handle bad input with try and except in a calculator Learn Mini project: Safe calculator in this Error Handling lesson with runnable Python examples on..."
order: 114
chapter: 16
chapterTitle: "Error Handling"
level: beginner
quickAnswer: "Handle bad input with try and except in a calculator. This PyGuide lesson shows Mini project: Safe calculator with examples you can run in the browser."
---

## Why this matters

Mini project: Safe calculator shows up throughout Error Handling and later chapters. Understanding it here makes the next lessons easier and helps you read real Python code with confidence. PyGuide keeps each page focused on one idea so you can practice without getting lost.


A calculator should not crash when someone types **hello** instead of a number. Wrap risky steps in **`try` / `except`**.

## Simulated safe divide

<PythonExample
  filename="safe_calc.py"
  code={`def safe_divide(a_text, b_text):
    try:
        a = float(a_text)
        b = float(b_text)
        result = a / b
    except ValueError:
        return "Please enter numbers"
    except ZeroDivisionError:
        return "Cannot divide by zero"
    else:
        return f"Result: {result}"

print(safe_divide("10", "2"))
print(safe_divide("10", "0"))
print(safe_divide("ten", "2"))`}
/>

**Line by line**

- **`ValueError`** — bad number text
- **`ZeroDivisionError`** — divide by zero
- **`else`** — only when no error

## Stretch goals

- Chain operations (+, -, \*)
- Loop until user types `quit`
- Log errors to a list for debugging

## Recap

- **`try`** around conversion and math
- **Specific `except`** types give clear messages
- Real tools stay running when users make mistakes
