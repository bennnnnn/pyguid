---
title: "Type hints intro"
description: "Python type hints intro for beginners: annotate variables and functions for clearer code. Learn basic syntax without advanced typing theory."
order: 193
chapter: 40
chapterTitle: "Type Hints"
section: "Type hints basics"
part: 9
partTitle: "Advanced Python"
level: beginner
quickAnswer: "Type hints add optional annotations like name: str and def greet(name: str) -> None to document expected types; Python does not enforce them at runtime by default."
---

## Why this matters

Hints help editors autocomplete and catch mistakes with tools like mypy as projects grow. They appear in modern library docs and team codebases. Starting with simple str and int annotations builds habits without blocking beginners.

Labels on storage bins say “integers” or “text.” **Type hints** label what a function **expects** and **returns** — they do not change how Python runs your code at beginner level.

## One new idea: colon types

<PythonExample
  filename="hints.py"
  code={`def greet(name: str) -> None:
    print(f"Hello, {name}")

def add(a: int, b: int) -> int:
return a + b

print(add(2, 3))`}
/>

| Part        | Meaning                |
| ----------- | ---------------------- |
| `name: str` | expect text            |
| `-> int`    | returns an integer     |
| `-> None`   | returns nothing useful |

## Why bother?

- Editors can **highlight** wrong types
- Readers see intent without guessing
- Same syntax in **2026** tutorials and jobs

<Callout variant="note">
  Python **does not** enforce hints at runtime by default. Wrong types can still run until
  they cause an error. Tools like **mypy** check later — optional for you now.
</Callout>

## Recap

- **`param: type`** and **`-> return_type`**
- Documentation for humans and tools
- Not required on every beginner function — use on new code when easy
