---
title: "Mini project: Contact book"
description: "Store names and phones in a dict with lookup and add Learn Mini project: Contact book in this Dictionaries lesson with runnable Python examples on PyGuide...."
order: 99
chapter: 13
chapterTitle: "Dictionaries"
level: beginner
quickAnswer: "Store names and phones in a dict with lookup and add. This PyGuide lesson shows Mini project: Contact book with examples you can run in the browser."
---

## Why this matters

Mini project: Contact book shows up throughout Dictionaries and later chapters. Understanding it here makes the next lessons easier and helps you read real Python code with confidence. PyGuide keeps each page focused on one idea so you can practice without getting lost.

A paper address book maps **name → phone**. Your **contact book** uses a **dict**, **`get`**, **`setdefault`**, and loops — the same pattern as real contact apps (simplified).

## Core contact book (browser-safe)

<PythonExample
  filename="contacts.py"
  code={`contacts = {}

def add_contact(name, phone):
contacts[name] = phone

def lookup(name):
return contacts.get(name, "Not found")

add_contact("Ada", "555-0100")
add_contact("Bob", "555-0101")
print(lookup("Ada"))
print(lookup("Zoe"))

for name, phone in contacts.items():
print(f"{name}: {phone}")`}
/>

## Safe add without overwriting

<PythonExample
  filename="setdefault_add.py"
  code={`contacts = {}

def add_if_new(name, phone):
if name in contacts:
print("Already exists:", name)
return
contacts[name] = phone

add_if_new("Ada", "555-0100")
add_if_new("Ada", "555-9999")
print(contacts)`}
/>

## Stretch goals

- **`pop`** to delete a contact
- List all names sorted with **`sorted(contacts.keys())`**
- Save/load JSON (see **JSON todo** project)

## Recap

- **Dict** = contact book
- **`get`** for safe lookup
- **`items()`** to print everything
