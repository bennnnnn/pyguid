---
title: "Password hashing"
description: "Password hashing in FastAPI — bcrypt, passlib, and safe storage for user credentials."
quickAnswer: "Never store plain passwords—hash with bcrypt (via passlib) and verify with `verify(plain, hash)` on login."
order: 154
sectionId: "authentication"
sectionTitle: "Authentication"
sectionOrder: 28
lessonOrder: 3
level: beginner
---

## Why this matters

Database leaks happen. Hashed passwords slow attackers; plain text passwords become instant account takeovers. Hashing is one-way—you cannot recover the password, only verify a login attempt.

## Hash and verify

<CodeExample language="python" filename="main.py" runnable={true} code={`from passlib.context import CryptContext

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

def hash_password(password: str) -> str:
    return pwd_context.hash(password)

def verify_password(plain: str, hashed: str) -> bool:
    return pwd_context.verify(plain, hashed)`} />

<CodeExample language="bash" filename="terminal.sh" runnable={true} code={`pip install passlib[bcrypt]`} />

<Callout variant="warning">

Do not invent your own salt or hash algorithm—use bcrypt or argon2 via established libraries.

</Callout>

## Recap

- Store only password hashes
- bcrypt via passlib is a common choice
- `verify` on login, `hash` on signup/password change

## Up next

When you are ready, continue with **JWT tokens** (use **Next** below). Each lesson builds on the previous one in this track.
